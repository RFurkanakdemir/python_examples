import numpy as np   #numpy kütüphane bildirimi

result=np.array([1,3,5,7,9])

result=np.arange(1,10)
result=np.arange(10,100,3)   #10-100 arası 3 erli
result=np.zeros(10)     #0 dizisi olşturur
result=np.ones(10)      #1 dizisi oluşturur
result=np.linspace(0,100,5)   # 0-100 arasını 5 parçaya böl
result=np.random.randint(0,10) #0-9 arası rastgele sayı üretir
result=np.random.rand(5)            #0-1 arası 5 sayı üreyt




